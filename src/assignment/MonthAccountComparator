package DTO;
import java.util.Comparator;
import managerstudent.Employee;

public class MonthAccountComparator implements Comparator<Employee>{
    @Override
    public int compare(Employee o1, Employee o2) {
        int result = Comparator.comparing((Employee e) -> e.getCode())
        .thenComparing(s -> e.getName())
        .compare(o1, o2);
     return result;
    }
}
